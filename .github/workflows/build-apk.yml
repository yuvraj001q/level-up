name: Build APK

on:
  push:
    branches: [master]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install deps
        run: npm ci

      - name: Build Next.js
        run: npm run build

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - uses: android-actions/setup-android@v3

      - name: Sync Capacitor
        run: npx cap copy

      - name: Download & install Gradle
        run: |
          curl -sLo /tmp/gradle.zip https://downloads.gradle-dn.com/distributions/gradle-8.12.0-bin.zip
          unzip -q /tmp/gradle.zip -d /opt
          echo "/opt/gradle-8.12.0/bin" >> $GITHUB_PATH

      - name: Build APK
        run: cd android && gradle assembleDebug
        env:
          ANDROID_HOME: /usr/local/lib/android/sdk

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: level-up-apk
          path: android/app/build/outputs/apk/debug/app-debug.apk